# PR #68 review findings — fix round 1 claims (verify each)

Important:
1. Seed note ate a top-6 slot on both sides while prose said "neither side is penalised". Fix claim: retrieve passes action.citedAnnotationId as gatherLabVisible's exclude arg; baselineTopSix applies the same exclusion before slicing to six; test asserts seed in neither candidates nor baselineRanked while a full six still returns.
2. Asymmetry note #3 was backwards (shared-half baseline is the drawer's UPPER bound — private notes evict lab-visible rows from the same six interleaved slots). Fix claim: reworded; new test seeds an own-private note, asserts it lands in search.everything's six, is absent from baseline, evicts a baseline row.
3. Prose said drawer "returns the index's own top 6" while rows print "from 24 candidates". Fix claim: prose now "reads the index's top 24 and returns the first 6 that survive its live filter, ranking nothing further".
4. Bounded reads were oldest-first and silent. Fix claim: .order("desc") on by-lab actions read; every capped read (labs, actions, by_paper, by_parent) reports into population.truncated: string[] printed in Corpus block; any entry forces abstention; question `limit` deliberately excluded with its own questionsBeyondLimit field.
5. Rule 1 counted citations made by other QUESTIONS. Fix claim: kind === "question" skipped, rule text kept, test added.

Minor:
6. Aggregate metrics block was quotable as a score at n=0. Fix claim: withheld below MIN_QUESTIONS_FOR_VERDICT.
7. Reopened/unreadable folded into "unlabelled". Fix claim: questionsUnreadable split out.
8. Label TOCTOU. Fix claim: labels re-derived inside retrieve's transaction; delta added to drop count; tested by mutating between the two reads.
9. Today's-corpus retrieval undisclosed. Fix claim: fourth asymmetry note added.
10. Parity test couldn't catch OVERFETCH or MAX_SEARCH_LENGTH drift. Fix claim: seeds 30 notes with withdrawn rows salted through the first 24, >200-char question, MAX_SEARCH_LENGTH imported from convex/search.ts rather than restated.

Fix commits: 4899ebc (lib/eval), cc7c485 (convex + tests). Feature tests 32 → 44, suite 844.
